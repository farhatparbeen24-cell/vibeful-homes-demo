import { HomePage } from "./home-client";

// Title & description come from the root metadata in src/app/layout.tsx
// ("Vibeful Homes | Everyday Products for Better Living").

export default function Page() {
  return <HomePage />;
}